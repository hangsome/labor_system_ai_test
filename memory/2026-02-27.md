# 2026-02-27

## Session Records

- Claude invocation: `claude -p --model opus "Reply exactly: OPUS_OK"` => `OPUS_OK`.
- Deployment decision change: switch to local deployment/manual verification first; postpone server deployment until whole project completion.
- Local build status: backend image built locally as `labor-system-backend:local-stage2` (success).
- Local frontend container build fallback: Docker Hub metadata request (`registry-1.docker.io`) intermittently EOF; fallback to local `npm` dev server for manual verification.
- Local deployment verification:
  - `docker compose` services `mysql/redis/rabbitmq/minio/backend` started.
  - backend health check `http://127.0.0.1:8080/actuator/health` => `200 {"status":"UP"}`.
  - frontend dev server `http://127.0.0.1:5173` reachable.
  - frontend proxy API `GET /api/admin/v1/iam/roles` => `200`.
- Config adjustments for local verification:
  - Added `BACKEND_PORT` mapping in compose.
  - Added Vite dev proxy (`/api`, `/actuator` -> `127.0.0.1:8080`).
  - Added local `.env` (development-only credentials/ports).
- Frontend restore routing (Phase 2 acceptance issue):
  - Gemini route attempt (dashboard 1:1 prototype restoration) via `python ... gemini_cli_bridge.py --file frontend/src/views/DashboardView.vue ...` produced no output and hung; process terminated and fallback to Codex immediately.
  - Fallback reason: Gemini call non-success behavior (hung/no response), so Codex took over implementation per routing policy.
- Claude verification (current session): `claude -p --model opus "Reply exactly: OPUS_OK"` => `OPUS_OK`.
- Product page fix completed: rewrote `frontend/src/views/DashboardView.vue` to match prototype style (top bar + left navigation + KPI cards + trend + customer distribution + todo + recent customers) while keeping existing route entries.
- Frontend regression after fix: `npm --prefix frontend run test` => `10 files / 22 tests passed`.
- Frontend build validation after dashboard restore: `npm --prefix frontend run build` => success (`vite build` done), with non-blocking chunk-size warning (`dist/assets/index-CGB_qyK5.js` ~1006.34 kB).
- Phase1/Phase2 frontend 1:1 restore completed (prototype-aligned) for: Login, Dashboard, SystemRole, AuditLog, LeadList/Detail, EmployerUnitList, ContractList/Detail.
- Frontend model routing policy updated to `Gemini -> Claude -> Codex` in workflow docs/patterns (`workflow-main`, `agent-routing`, `stage-3-task-decomposition`, `stage-4-execution`, `patterns/ops/llm-fallback-on-timeout-or-429`).
- Regression after restore: `npm --prefix frontend run test` => 10 files / 22 tests passed; `npm --prefix frontend run test:e2e` => 5 passed; `npm --prefix frontend run build` => success with non-blocking chunk-size warning.
- Acceptance follow-up fixes (this run): fixed detail-page back navigation fallback, enabled CRUD/button actions on Contract/SystemRole/AuditLog/Dashboard pages, and normalized visible status text to Chinese.
- Frontend verification (this run): `npm --prefix frontend run test` => 10 files / 22 tests passed; `npm --prefix frontend run build` => success; `npm --prefix frontend run test:e2e` => 5 passed.
- Runtime acceptance environment (this run): `docker compose up -d --build backend frontend` with dependency services healthy; backend health check `http://localhost:8080/actuator/health` => `{"status":"UP"}`; frontend reachable at `http://localhost:8088` (HTTP 200).
- DB credential alignment for containerized backend: reset MySQL `labor` account password to `.env` value (`labor_123`) to satisfy backend container datasource auth.
- Git policy honored: no commit performed; waiting for user acceptance.

