# 2026-02-27

## Session Records

- Claude invocation: `claude -p --model opus "Reply exactly: OPUS_OK"` => `OPUS_OK`.
- Deployment decision change: switch to local deployment/manual verification first; postpone server deployment until whole project completion.
- Local build status: backend image built locally as `labor-system-backend:local-stage2` (success).
- Local frontend container build fallback: Docker Hub metadata request (`registry-1.docker.io`) intermittently EOF; fallback to local `npm` dev server for manual verification.
- Local deployment verification:
  - `docker compose` services `mysql/redis/rabbitmq/minio/backend` started.
  - backend health check `http://127.0.0.1:8080/actuator/health` => `200 {"status":"UP"}`.
  - frontend dev server `http://127.0.0.1:5173` reachable.
  - frontend proxy API `GET /api/admin/v1/iam/roles` => `200`.
- Config adjustments for local verification:
  - Added `BACKEND_PORT` mapping in compose.
  - Added Vite dev proxy (`/api`, `/actuator` -> `127.0.0.1:8080`).
  - Added local `.env` (development-only credentials/ports).
