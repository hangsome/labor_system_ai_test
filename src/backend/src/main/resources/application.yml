spring:
  application:
    name: labor-system-backend
  datasource:
    url: jdbc:mysql://localhost:3306/labor_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: xu@736107#MH
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration,filesystem:../../database/migrations
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: labor
    password: labor123

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

