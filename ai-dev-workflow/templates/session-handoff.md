# Session Handoff — 会话交接文档

> 本文件在上下文压缩次数达到阈值（默认 10 次）时由 AI 自动生成。新会话中输入「继续」即可自动恢复。

## 元数据

- **项目名称**：<项目名>
- **当前 Phase**：Phase XX - <名称>
- **会话序号**：Session-<N>
- **中断时间**：<ISO8601>
- **压缩次数**：<N> / <threshold>

## 一键恢复提示词

> 📋 在新会话中输入以下内容即可恢复：

```
继续
```

或直接输入 `yes` / `continue`。

## 当前进度

- **当前 Phase**：Phase <XX> - <名称>
- **当前 Stage**：Stage <N>
- **执行中的任务 ID**：<PH01-XXX 或 "无">
- **任务状态**：<进行中 / 刚完成，未开始下一个>

## CSV 快照

```
已完成: <N> / <Total> 任务
进行中: <N> 任务
失败/阻塞: <N> 任务
下一个待执行: <任务 ID> - <标题>
```

## 关键决策记录

> 本会话中做出的重要技术/架构决策，防止新会话重复思考。

1. <决策 1>
2. <决策 2>
3. ...

## 阻塞与待解决

> 当前存在的阻塞项或需要用户确认的问题。

- <阻塞/待确认项>

## Git 状态

- **当前分支**：<feature/phase-XX-xxx>
- **最后 Commit**：<hash> - <message>
- **是否有未提交代码**：<否 — 会话切换前已自动提交>

## 自动恢复指令

> 以下步骤在用户输入恢复指令后由 AI **全自动执行**。

恢复序列：
1. 读取本文件获取中断上下文
2. 读取 `phases/phase-XX/todolist.csv` 获取任务状态
3. 读取 `phases/phase-XX/process.md` 获取阶段进度
4. 执行 Context Recovery（检查并清理半成品代码）
5. 从 `下一个待执行` 任务继续工作
6. **恢复完成后**：将本文件重命名为 `session-handoff-done-Session-<N>.md` 归档
